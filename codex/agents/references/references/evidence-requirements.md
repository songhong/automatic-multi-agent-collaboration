# Evidence Requirements

Every developer/tester should write evidence files under `.agent-work/evidence/<batch_id>/<agent-name>/attempt-<N>/`.

Examples:

- command logs
- server logs
- browser console logs
- screenshots
- interaction traces
- audit outputs
- metric JSON

If evidence cannot be produced, record a limitation and whether it blocks PASS.
